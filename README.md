# Laravel-Dashboard-Template-Makes-Your-Project

## Local Development

- `npm run dev` starts Vite and the fingerprint bridge together.
- `npm run dev:vite` starts Vite only.
- `npm run dev:bridge` starts the fingerprint bridge only.
